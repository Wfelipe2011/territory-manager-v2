<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard Admin - Territory Manager</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body { background-color: #121212; color: #E0E0E0; font-family: 'Inter', sans-serif; }
        .card { background-color: #1E1E1E; border-radius: 12px; padding: 1.5rem; }
    </style>
</head>
<body class="bg-[#121212] flex min-h-screen">
    {{> sidebar}}

    <main class="flex-1 md:ml-64 p-4 md:p-8">
        <div class="max-w-6xl mx-auto">
            <!-- Mobile Header -->
            <div class="flex md:hidden justify-between items-center mb-6">
                <button onclick="toggleSidebar()" class="text-gray-400 p-2 hover:bg-gray-800 rounded-lg">
                    <i class="fas fa-bars text-xl"></i>
                </button>
                <h1 class="text-lg font-bold text-white">Dashboard Admin</h1>
                <div class="w-8"></div>
            </div>

            <header class="hidden md:flex justify-between items-center mb-10">
                <div>
                    <h1 class="text-3xl font-bold text-white">Visão Geral do Sistema</h1>
                    <p class="text-gray-400 mt-1">Métricas de negócio e atividade global</p>
                </div>
            </header>

            <!-- Metrics Grid -->
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-10">
                <div class="card border-l-4 border-blue-500">
                    <div class="flex items-center gap-4">
                        <div class="w-12 h-12 bg-blue-500/10 rounded-full flex items-center justify-center text-blue-500">
                            <i class="fas fa-building text-xl"></i>
                        </div>
                        <div>
                            <p class="text-gray-400 text-xs uppercase font-bold tracking-wider">Congregações Ativas</p>
                            <h2 class="text-2xl font-bold text-white">{{metrics.activeTenants}}</h2>
                        </div>
                    </div>
                </div>

                <div class="card border-l-4 border-green-500">
                    <div class="flex items-center gap-4">
                        <div class="w-12 h-12 bg-green-500/10 rounded-full flex items-center justify-center text-green-500">
                            <i class="fas fa-users text-xl"></i>
                        </div>
                        <div>
                            <p class="text-gray-400 text-xs uppercase font-bold tracking-wider">Usuários Ativos</p>
                            <h2 class="text-2xl font-bold text-white">{{metrics.totalUsers}}</h2>
                        </div>
                    </div>
                </div>

                <div class="card border-l-4 border-purple-500">
                    <div class="flex items-center gap-4">
                        <div class="w-12 h-12 bg-purple-500/10 rounded-full flex items-center justify-center text-purple-500">
                            <i class="fas fa-map-marked-alt text-xl"></i>
                        </div>
                        <div>
                            <p class="text-gray-400 text-xs uppercase font-bold tracking-wider">Territórios Ativos</p>
                            <h2 class="text-2xl font-bold text-white">{{metrics.totalTerritories}}</h2>
                        </div>
                    </div>
                </div>

                <div class="card border-l-4 border-yellow-500">
                    <div class="flex items-center gap-4">
                        <div class="w-12 h-12 bg-yellow-500/10 rounded-full flex items-center justify-center text-yellow-500">
                            <i class="fas fa-chart-line text-xl"></i>
                        </div>
                        <div>
                            <p class="text-gray-400 text-xs uppercase font-bold tracking-wider">Taxa Atividade</p>
                            <h2 class="text-2xl font-bold text-white">{{metrics.activeTenants}}/{{metrics.totalTenants}}</h2>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Second Row -->
            <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
                <div class="lg:col-span-2 card">
                    <h3 class="text-lg font-bold text-white mb-6">Status dos Dados Ativos</h3>
                    <div class="space-y-6">
                        <div>
                            <div class="flex justify-between text-sm mb-2">
                                <span class="text-gray-400">Casas em Uso</span>
                                <span class="text-white font-mono">{{metrics.totalHouses}}</span>
                            </div>
                            <div class="w-full bg-gray-800 rounded-full h-2">
                                <div class="bg-blue-500 h-2 rounded-full" style="width: 100%"></div>
                            </div>
                        </div>
                         <div>
                            <div class="flex justify-between text-sm mb-2">
                                <span class="text-gray-400">Arrecadação (Congregações Ativas)</span>
                                <span class="text-green-400 font-mono">R$ {{formatCurrency metrics.globalBalance}}</span>
                            </div>
                            <div class="w-full bg-gray-800 rounded-full h-2">
                                <div class="bg-green-500 h-2 rounded-full" style="width: 100%"></div>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="card flex flex-col items-center justify-center text-center">
                    <div class="w-20 h-20 bg-green-500/10 rounded-full flex items-center justify-center text-green-500 mb-4">
                        <i class="fas fa-check-circle text-4xl"></i>
                    </div>
                    <h3 class="text-xl font-bold text-white">Sistema Online</h3>
                    <p class="text-gray-400 mt-2 text-sm">Todos os serviços estão operando normalmente em todas as regiões.</p>
                    <a href="/v1/dashboard/health" class="mt-6 px-4 py-2 bg-gray-800 hover:bg-gray-700 rounded-lg text-sm transition-colors">Ver Saúde Técnica</a>
                </div>
            </div>
        </div>
    </main>
</body>
</html>
