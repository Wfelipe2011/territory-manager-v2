<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Trocar Tenant - Territory Manager</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body {
            background-color: #121212;
            color: #E0E0E0;
            font-family: 'Inter', sans-serif;
        }
    </style>
</head>
<body class="bg-[#121212] flex min-h-screen">
    {{> sidebar}}

    <main class="flex-1 md:ml-64 p-4 md:p-8">
        <div class="max-w-4xl mx-auto">
            <!-- Mobile Header with Menu Toggle -->
            <div class="flex md:hidden justify-between items-center mb-6">
                 <button onclick="toggleSidebar()" class="text-gray-400 p-2 hover:bg-gray-800 rounded-lg">
                    <i class="fas fa-bars text-xl"></i>
                </button>
                <h1 class="text-lg font-bold text-white">TM Health</h1>
                <div class="w-10"></div>
            </div>

            <header class="mb-8 flex justify-between items-center">
                 <div>
                    <h1 class="text-3xl font-bold text-white">Trocar de Congregação</h1>
                    <p class="text-gray-400 mt-1">Super Admin Mode</p>
                </div>
                <a href="/v1/dashboard" class="hidden md:flex text-gray-400 hover:text-white items-center gap-2 transition-colors">
                    <i class="fas fa-arrow-left"></i>
                    <span>Voltar ao Dashboard</span>
                </a>
            </header>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                {{#each tenants}}
                <form action="/v1/dashboard/switch-tenant" method="POST">
                    <input type="hidden" name="tenantId" value="{{this.id}}">
                    <button type="submit" class="w-full text-left bg-[#1E1E1E] hover:bg-gray-800 border {{#if (eq this.id ../user.tenantId)}}border-green-500 bg-green-500/5{{else}}border-gray-800{{/if}} rounded-xl p-6 transition-all group">
                        <div class="flex justify-between items-center">
                            <div>
                                <h3 class="text-lg font-bold text-white group-hover:text-green-400">{{this.name}}</h3>
                                <p class="text-gray-500 text-sm mt-1">ID: {{this.id}} | Cidade: {{this.city}}</p>
                            </div>
                            {{#if (eq this.id ../user.tenantId)}}
                            <span class="bg-green-600 text-white text-[10px] uppercase font-bold px-2 py-1 rounded">Ativo</span>
                            {{else}}
                            <i class="fas fa-chevron-right text-gray-700 group-hover:text-gray-500 transition-colors"></i>
                            {{/if}}
                        </div>
                    </button>
                </form>
                {{/each}}
            </div>
        </div>
    </main>
</body>
</html>
